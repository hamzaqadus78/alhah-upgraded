/**
 * Shop API base URL. Update this one line once the backend (server/) is
 * deployed (e.g. Railway/Render) — see plan Part B7.
 */
window.ALHAH_SHOP_CONFIG = {
  API_BASE: 'http://localhost:3000',
};
